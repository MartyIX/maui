<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Maui.Controls.Sample"
    x:Class="Maui.Controls.Sample.MainPage"
    x:DataType="local:MainPage">

    <FlexLayout BindableLayout.ItemsSource="{Binding CardNames}" Direction="Row" AlignItems="Center" JustifyContent="SpaceEvenly" Wrap="Wrap">
        <BindableLayout.ItemTemplate>
            <DataTemplate>
                <Grid BackgroundColor="#555555">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Image Grid.Row="0" Aspect="AspectFit" HeightRequest="200" WidthRequest="300"/>

                    <Label Grid.Row="1" Text="{Binding}" HorizontalTextAlignment="Center"/>
                </Grid>
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </FlexLayout>

</ContentPage>